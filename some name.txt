sfsoinfsf

git config --global user.name "name"
git config --global user.email "email"

git cd (directory) 		(starts in users)
cd source

git clone (URL) 	creates a repository/copies a repository

git status 		shows files found within repository

git add testfile.txt  	(use "\ " to identify a space)

git commit -m "your message"	(adds a message) (commits the selected files)

git push origin master 		(sends it into github)

git pull 			(pulls the changes from github)

I AM A COOKIE





